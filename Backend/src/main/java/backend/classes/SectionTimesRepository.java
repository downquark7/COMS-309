package backend.classes;

import org.springframework.data.jpa.repository.JpaRepository;

/***
 * section times jpa repository interface
 */
public interface SectionTimesRepository extends JpaRepository<SectionTime, Integer>
{
}
