package backend.classes;

import org.springframework.data.jpa.repository.JpaRepository;

/***
 * section table jpa repository
 */
public interface SectionRepository extends JpaRepository<Section, Integer>
{
}
